using UnityEngine;
using System.Collections;

public class Instructions : MonoBehaviour {

	// Use this for initialization
	public bool showGUI = true;
	/*
	void OnGUI()
	{if (showGUI) {
			GUI.Box (new Rect (Screen.width - 245, 10, 240, 150), "M: Show/Hide Instructions");
			GUI.Label (new Rect (Screen.width - 235, 30, 240, 30), "Up/Down Arrow : Go Forwald/Go Back");
			GUI.Label (new Rect (Screen.width - 235, 50, 240, 30), "Left/Right Arrow : Turn Left/Turn Right");
			GUI.Label (new Rect (Screen.width - 235, 70, 240, 30), "Space Key: Jump");
			GUI.Label (new Rect (Screen.width - 235, 90, 240, 30), "1: Punch");
			GUI.Label (new Rect (Screen.width - 235, 110, 240, 30), "2: Kick");
			GUI.Label (new Rect (Screen.width - 235, 130, 240, 30), "3: Turn and Kick");
		}

	}

	// Update is called once per frame
	void Update () {
		if (Input.GetKeyDown(KeyCode.M)) {
			print("M key was pressed");
			showGUI = !showGUI;
		}
	}*/
}
